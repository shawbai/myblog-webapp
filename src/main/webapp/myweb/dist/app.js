require.config({
    baseUrl: '../dist',
    paths: {
    	 jquery : "vendors/jquery/jquery",
    	 layer : "widget/layer/layer",
         hoss: "scripts/hoss",
         xhr: "scripts/xhr",
         login: "scripts/login",
    }
});
